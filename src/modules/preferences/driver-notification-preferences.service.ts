export class DriverNotificationPreferencesService {
  // TODO: Read preferences from existing `driver_notification_preferences` source.
  async getPreferencesForDriver(): Promise<Record<string, boolean>> {
    return {};
  }
}
